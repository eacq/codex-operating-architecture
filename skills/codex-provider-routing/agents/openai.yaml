interface:
  display_name: "Provider Routing (Legacy Alias)"
  short_description: "Compatibility alias for credential-management provider routing"
  default_prompt: "Use $codex-credential-management-provider-routing to diagnose my locally configured OpenAI-compatible provider without exposing credentials."
