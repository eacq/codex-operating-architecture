interface:
  display_name: "Academic Figure Optimizer"
  short_description: "Standardize academic figures, fonts, arbitrary dimensions, resolution, and exports."
  default_prompt: "Use $academic-figure-optimizer to optimize this scientific figure while preserving numerical fidelity."

policy:
  allow_implicit_invocation: true
