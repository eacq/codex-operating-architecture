---
name: your-api-source
description: Deprecated compatibility alias for codex-credential-management-provider-routing. Use only when an existing prompt or local task names this historical skill.
---

# Compatibility Alias

`your-api-source` was renamed to `$codex-credential-management-provider-routing`
because the capability is owned by credential management and routes rather than
selects OpenAI-compatible providers. Use the parent-prefixed name for all new tasks.

This alias remains available for one release cycle. It owns no scripts,
configuration, credentials, or independent workflow.
