# v1.0.1.1

## English

Verified automatic refinement: every eligible file-organization iteration now creates and verifies an off-root backup before moving managed inbox files, analyzes supported references/configuration, applies exact safe rewrites, and runs configured validation commands.

## 中文对照

经验证的自动精炼：每次符合条件的文件整理迭代现在会在移动受管理收件箱文件前创建并校验根目录外备份，分析受支持的引用与配置，执行精确安全改写，并运行配置的验证命令。
