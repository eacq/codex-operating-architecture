# v1.1.1.0

## English

- Installed `codebase-memory-mcp` as a local Codex MCP server backed by a checksum-verified Windows amd64 release.
- Added graph-first repository orientation guidance to `codex-information-gathering`.
- Recorded Codebase Memory MCP as a verified local knowledge concept with cache, privacy, and verification boundaries.
- Indexed the canonical architecture repository in fast mode and verified graph search against the current Git head.

## Chinese / 中文

- 将 `codebase-memory-mcp` 安装为本地 Codex MCP server，并使用经过 SHA-256 校验的 Windows amd64 release。
- 在 `codex-information-gathering` 中加入代码图谱优先的仓库取证规则。
- 将 Codebase Memory MCP 记录为已验证的本地知识能力，并明确缓存、隐私和验证边界。
- 使用 fast 模式索引规范架构仓库，并基于当前 Git head 验证图谱检索。
