# v1.2.1.0

## English

- Add an MCP startup preflight to `codex-self-evolution`.
- At source-repository project entry, the lifecycle now proactively runs fast
  `index_repository` when `codebase-memory-mcp` tools are exposed.
- If the current Codex task does not expose the MCP tools, the workflow records
  the gap and falls back to local file evidence.

## Chinese / 中文

- 为 `codex-self-evolution` 新增 MCP 启动预热子 skill。
- 源码仓库项目入口在 `codebase-memory-mcp` 工具已暴露时，会主动运行 fast `index_repository`。
- 若当前 Codex 任务未暴露 MCP 工具，流程会记录该缺口并回退到本地文件取证。
