# v2.1.0.1

## English

Verified private synchronization recovery refinement.

- Preserve the `2.1` private synchronization version line after the release commit.
- Route release-publication remote reference probes through the scoped GitHub network helper.
- Keep the recovery path covered by the publication-resume contract test and experience-version regression tests.

## Chinese / 中文

已验证的私有同步恢复修复。

- 在发布提交后保持 `2.1` 私有同步版本线。
- 将发布恢复中的远端引用探测统一路由到受控 GitHub 网络 helper。
- 用发布恢复合同测试和经验版本回归测试覆盖该恢复路径。
