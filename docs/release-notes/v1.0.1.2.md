# v1.0.1.2

## English

Verified refinement: all non-protected folders are eligible for managed organization through project policy. Before Git staging, tracked paths and affected tracked reference/configuration files are restored from the local-only move manifest; secrets remain in the secure store and only credential references may be updated.

## 中文对照

经验证的精炼：所有非受保护目录均可通过项目策略进入受管理整理范围。Git 暂存前，受跟踪路径及受影响的受跟踪引用/配置文件会从仅本地迁移清单恢复；秘密仍保留在安全存储中，只允许更新凭据引用。
