# File-organization concept image / 文件整理示意图溯源

The bitmap remains a privacy-safe conceptual overview. The deterministic Mermaid diagram is authoritative for the expanded cleanup, post-replacement validation, and lifecycle-writeback sequence; no bitmap regeneration is required because the concept image semantics did not change.

位图继续作为隐私安全的概念示意；扩展后的隔离清理、替换后验证和生命周期写回顺序以确定性的 Mermaid 图为准。由于概念图语义未改变，本次无需重新生成位图。

## English

- Asset: `file-organization-concept.png`.
- Purpose: explain the privacy-safe lifecycle relationship among intake, active work, references, outputs, archive, private-local material, backup, and iteration review.
- Generation policy: a sanitized GPT-generated image is preferred because the relationship is non-linear; the Mermaid architecture diagram remains the deterministic fallback.
- Prompt boundary: no personal names, local paths, filenames, credentials, repository identities, file contents, or account information.
- Maintenance: retain the image when topology and meaning remain correct; edit it for a localized visual correction; regenerate it after topology changes; delete it when a visual no longer improves comprehension. Update both READMEs, this provenance record, and the Mermaid diagram in the same iteration.

## 中文对照

- 资源：`file-organization-concept.png`。
- 用途：说明收集、进行中工作、参考资料、输出、归档、本地私有资料、备份与迭代审查之间的隐私安全生命周期关系。
- 生成策略：由于关系为非线性且能显著提升理解，优先使用脱敏 GPT 生图；Mermaid 架构图保留为确定性回退。
- 提示词边界：不得包含个人姓名、本地路径、文件名、凭据、仓库身份、文件内容或账户信息。
- 维护：拓扑和语义仍正确时保留；局部视觉问题时修图；拓扑变化时重新生成；图像不再提升理解时删除。同一次迭代中必须同步两个 README、本溯源记录和 Mermaid 图。
