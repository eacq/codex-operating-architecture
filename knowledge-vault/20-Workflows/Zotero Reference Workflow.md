---
id: workflow-zotero
type: workflow
status: active
source: memory-zotero-api-setup
verified: true
learning_audience: codex
codex_learning: Recover the Zotero numeric user ID from /keys/<key> metadata before calling /users/{id}; never record the key itself.
---

# Zotero Reference Workflow

Use Zotero as the bibliographic authority for [[Literature Discovery Workflow]], [[Paper Reading and Evidence Workflow]], and [[Academic Writing and Citation Workflow]]. Recover the numeric user ID from the key metadata endpoint before `/users/{id}` calls. Store credential names and verification methods, never secret values.
